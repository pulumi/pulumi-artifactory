// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides an Artifactory Trash Can configuration resource. This can be used to create and manage Artifactory trash can configuration.
 *
 * This resource configuration corresponds to `trashcanConfig` config block in system configuration XML
 * (REST endpoint: [artifactory/api/system/configuration](https://www.jfrog.com/confluence/display/JFROG/Artifactory+REST+API#ArtifactoryRESTAPI-GeneralConfiguration)).
 *
 * When enabled, deleted items are stored in the trash can for the specified retention period before being permanently deleted.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as artifactory from "@pulumi/artifactory";
 *
 * const my_trash_can_config = new artifactory.TrashcanConfig("my-trash-can-config", {
 *     enabled: true,
 *     retentionPeriodDays: 14,
 * });
 * ```
 *
 * ## Import
 *
 * Current Trash Can configuration can be imported using any arbitrary value as the `ID`, e.g.
 *
 * ```sh
 * $ pulumi import artifactory:index/trashcanConfig:TrashcanConfig my-trash-can-config my-trash-can-config
 * ```
 */
export class TrashcanConfig extends pulumi.CustomResource {
    /**
     * Get an existing TrashcanConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TrashcanConfigState, opts?: pulumi.CustomResourceOptions): TrashcanConfig {
        return new TrashcanConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'artifactory:index/trashcanConfig:TrashcanConfig';

    /**
     * Returns true if the given object is an instance of TrashcanConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TrashcanConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TrashcanConfig.__pulumiType;
    }

    /**
     * If set, trash can will be enabled and deleted items will be stored in the trash can for the specified retention period. Defaults to `true`.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * The number of days to keep deleted items in the trash can before deleting permanently. Defaults to `14`.
     */
    declare public readonly retentionPeriodDays: pulumi.Output<number>;

    /**
     * Create a TrashcanConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: TrashcanConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TrashcanConfigArgs | TrashcanConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TrashcanConfigState | undefined;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["retentionPeriodDays"] = state?.retentionPeriodDays;
        } else {
            const args = argsOrState as TrashcanConfigArgs | undefined;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["retentionPeriodDays"] = args?.retentionPeriodDays;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TrashcanConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TrashcanConfig resources.
 */
export interface TrashcanConfigState {
    /**
     * If set, trash can will be enabled and deleted items will be stored in the trash can for the specified retention period. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * The number of days to keep deleted items in the trash can before deleting permanently. Defaults to `14`.
     */
    retentionPeriodDays?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a TrashcanConfig resource.
 */
export interface TrashcanConfigArgs {
    /**
     * If set, trash can will be enabled and deleted items will be stored in the trash can for the specified retention period. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * The number of days to keep deleted items in the trash can before deleting permanently. Defaults to `14`.
     */
    retentionPeriodDays?: pulumi.Input<number>;
}
